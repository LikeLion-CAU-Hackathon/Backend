spring:
  config:
    import: optional:classpath:application-jwt.yml
  datasource:
    url: jdbc:mysql://santa-db-dev.crgcm8c448r8.ap-northeast-2.rds.amazonaws.com:3306/santa_dev?serverTimezone=Asia/Seoul&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: santa
    password: ${RDS_PW}
    driver-class-name: com.mysql.cj.jdbc.Driver

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug

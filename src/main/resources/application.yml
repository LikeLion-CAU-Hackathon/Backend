spring:
  profiles:
    active: local
  config:
    import:
      #- optional:classpath:application-jwt.yml
      - optional:classpath:application-secret.yml

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: GOOGLE_CLIENT_ID
            client-secret: GOOGLE_CLIENT_SECRET
            scope:
              - email
              - profile
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

#  datasource:
#    url: jdbc:h2:tcp://localhost/~/santa
#    driver-class-name: org.h2.Driver
#    username: sa
#    password:
#
#  h2:
#    console:
#      enabled: true
#      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true # ★★★ 이 줄을 추가하세요

  sql:
    init:
      mode: embedded

server:
  forward-headers-strategy: framework


# jwt:
  # secret: ${jwt.secret}
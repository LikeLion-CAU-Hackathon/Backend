spring:
  jpa:
    # (테스트 환경) JPA가 테이블을 모두 만든 후 SQL을 실행하도록 지연
    defer-datasource-initialization: true
    hibernate:
      # (테스트 환경) 테스트가 실행될 때마다 DB를 새로 만들고 삭제
      ddl-auto: create-drop

  sql:
    init:
      # (테스트 환경) H2 같은 내장 DB를 쓸 때만 data.sql을 실행
      mode: embedded